export class Controller
{
	// example function
	// NOTE: using static allows you to do Controller.moveController() in App.js
	// 		 so we don't have to import each function individually
	static moveController(model)
	{
		// draw stuff
	}
}